import React from 'react';
import TransitionComponent from '../components/TransitionComponent/TransitionComponent';

const Homepage = (props) => {
	return (
		<TransitionComponent>
			<div className="box">This is Homepage</div>
    </TransitionComponent>
	);
};
export default Homepage;
